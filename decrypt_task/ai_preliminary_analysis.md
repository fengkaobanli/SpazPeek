# AI 初步分析（仅供参考 —— 含已知疑点，请谨慎采信）

> 生成：2026-09-26 03:50
> 状态：**未完成分析**。用户叫停后整理，仅作线索。**结论均未验证，可能存在伪相关。**

---

## 0. 一句话现状

用户反馈：**20ms 高速模式（47fps）"头部方向乱"**。
初步扫描发现了一些有趣的相关性（有信号），但存在"大 lag"和"跨轴重叠"两大疑点，**需要更严谨的方法复核**。

---

## 1. 实测帧长分布（本次会话 03:28:53~03:44:53）

```
长度      条数        说明
81B      14138+     单块帧（主流，占绝大多数）
145B     100        2块
209B     70         3块
273B     57         4块
337B     62         5块
401B     47         6块
465B     47         7块
529B     30         8块
593B     140        9块
（另有少量 8~22B 的 AACP 控制包、340B/255B 等个别异常帧）
```

## 2. 三阶段字节行为对比（重要线索）

用"逐字节多帧统计"看三个阶段（每阶段取40~60帧 81B 帧）：

| 阶段 | 时间 | 模式 | 活跃字节区 |
|------|------|------|-----------|
| A | 03:28:53~59 | 慢速（1.6fps） | b48-b63 最活跃(14/16)，b32-47 次之(10/16) |
| B | 03:29:05~10 | 40ms 档（24fps） | b48-b63 最活跃(15/16)，b32-47 次之(10/16) |
| C | 03:30:00~10 | 20ms 档（47fps） | b48-b63(11/16)，b36-47(8/16)，b64-79(10/16) |

### ⚠️ 发现的关键字节差异（A/B vs C）：

```
位置：b33~b36 区域（块 0 内的 offset 20~23）
  阶段 A/B： 03 00 43  
  阶段 C  ： 03 04 83          ← "43"→"83"（bit7 置位），另有 "00"→"04"

含义未知。疑似：数据模式标志（0x80 位可能是"高速/不同数据类型"标志）
```

## 3. 偏移 × XM5 相关性扫描（全动作窗口 03:31:40~32:26，1990 帧）

对每个偶数字节偏移取 int16 LE，与 XM5 三轴（q1=上下、q2=倾斜、q3=左右）相关：

```
最强命中（无平滑、最近邻对齐）：
  b[67] ↔ q1 （上下）: r = -0.853
  b[71] ↔ q2 （倾斜）: r = -0.809
  b[43] ↔ q3 （左右）: r = +0.761
  b[69] ↔ q2 （倾斜）: r = +0.731
  b[45] ↔ q1 （上下）: r = -0.683
  b[47] ↔ q3 （左右）: r = +0.503
```

## 4. 平滑 + 时间偏移（lag）搜索（全动作窗口，2189 帧）

平滑窗口 ±5 帧，lag 搜索 ±2s（步 50ms），线性插值对齐：

```
b[67] ↔ q1: lag=-600ms r=-0.933   ← 强，但 lag 高达 600ms（可疑）
b[71] ↔ q2: lag=-500ms r=-0.878
b[43] ↔ q3: lag=-100ms r=+0.758
b[45] ↔ q1: lag=-400ms r=-0.617
```

## 5. 分动作纯净窗口扫描（最关键，但也最矛盾）

对三个动作窗口分别扫描（lag 搜索 ±500ms）：

```
【动作1 左右转头 03:31:43.5~55.5】
  q3(左右):  b43=+0.99@-50ms   b47=+0.99@-50ms     ← 近乎完美！
  q1(上下):  b43=+0.94@+100ms
  q2(倾斜):  b43=-0.94@-150ms

【动作2 抬头低头 03:31:57.5~09.5】
  q1(上下):  b67=-0.97@-500ms  b69=-0.86@-400ms   ← 强（但 lag 500ms）
  q2(倾斜):  b67=-0.95@-500ms  b69=-0.84@-200ms

【动作3 左右歪头 03:32:11.5~23.5】
  q2(倾斜):  b45=+0.95@-50ms   b47=+0.99@+50ms    ← 近乎完美！
  q1(上下):  b47=-0.94@-150ms
  q2(倾斜):  b69=+0.97@-450ms  b71=-0.95@-500ms
```

## 6. ⚠️ 疑点清单（务必注意）

1. **大 lag 疑点**：动作2的信号出现在 -500ms —— 对于头追这种低延迟数据，500ms 的滞后极不自然。
   怀疑是"动作间的伪相关"（动作1/2/3都是正弦往复模式，大 lag 时可能跨动作对齐）
2. **跨轴重叠**：b47 在动作1 匹配 q3(0.99)，在动作3 匹配 q2(0.99)——同一字段对两轴都近乎完美，
   这不可能同时成立（除非窗口内 q2/q3 自身高度相关）
3. **q 轴自身耦合**：用户做单轴动作时，XM5 的其他轴也在动（人体自然耦合），
   导致"多轴同时高相关"。**必须检查 window 内 q1/q2/q3 的两两相关矩阵**（未做）
4. **未做多元回归**：应该对候选字段做 b ~ q1+q2+q3 的多元回归，看哪个轴的系数占主导（未做）
5. **未检查差分域**：也许应该在速度（差分）域做相关（未做）

## 7. 建议的后续分析路径（给接手者）

1. 先算每个动作窗口内 **XM5 三轴两两相关矩阵**（判断耦合程度）
2. 对每个候选偏移做 **多元回归/偏相关**，找出"独立解释各轴"的字段
3. 尝试 **"帧取模分组"** 假设：高速模式可能"每帧只发一个轴的增量"（轮转），
   可按帧序号 mod 3 分组后分别相关（未验证的猜想）
4. 比较 40ms 成功数据（reference/triple_calib_40ms_success.log）与 20ms 数据的
   **"同样偏移的数值分布"**，确认字段是否被重新布局
5. 检查 b33~b36 区域的 "4300→0483" 标志位是否与模式切换相关

## 8. 已知的物理量参考（来自 40ms 模式验证）

- AirPods 原始字段 o2/o3 是"两个倾斜通道"（±45° 安装的双传感器逻辑）：
  - pitch ≈ (o2n + o3n)/2 / 32000 × 180°
  - yaw   ≈ (o2n − o3n)/2 / 32000 × 180°
- 40ms 模式实测（与 XM5）：
  - 左右轴：AirPods↔XM5 r>0.99
  - 上下轴：r≈0.98~0.99
  - 倾斜轴：r≈0.99

---

*以上分析由 AI 于熄灯前抢救整理，未完成、未复核。请以独立分析为准。*
